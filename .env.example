# This template needs no secrets to run standalone.
#
# When you add app-specific config, expose it with the PUBLIC_ prefix so Rsbuild
# surfaces it on import.meta.env. This mirrors
# the reference host's convention, so the same var names work when deployed.
#
# PUBLIC_EXAMPLE_API_URL=
